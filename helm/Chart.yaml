apiVersion: v2
name: demo-app
description: A demo application showcasing GitOps CD with ArgoCD

# Chart version — bump this when the chart itself changes
version: 0.1.0

# App version — this is the default Docker image tag
# ArgoCD Image Updater will override this automatically with the latest pushed image
appVersion: "latest"
